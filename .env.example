# OFFLINE / LAN SOURCE MODE (developer/testing only)
APP_MODE=offline
APP_TIMEZONE=Asia/Kolkata
SECRET_KEY=change-this-before-real-use
SUPER_ADMIN_USERNAME=superadmin
LEGACY_ADMIN_USERNAME=admin
# Existing deployments can keep ADMIN_PASSWORD unchanged.
ADMIN_PASSWORD=Admin@123
# Optional: use a different password for the new Super Admin.
# SUPER_ADMIN_PASSWORD=AnotherStrongPassword
PORT=8080
# DATABASE_URL is intentionally omitted in offline mode. SQLite exam.db is used.
# The compiled V2.02 Windows release does NOT ship this default admin password;
# it uses a first-run administrator setup page instead.

# Staff/admin inactivity timeout
SESSION_TIMEOUT_MINUTES=45
